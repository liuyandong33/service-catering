<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="erp.chain.mappers.TenantMapper">
    <insert id="insert" parameterType="erp.chain.mappers.TenantMapper" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tenant(code, name, mobile,
        email, linkman, business,
        province_code, city_code, district_code,
        goods_table_name, sale_table_name, partition_code,
        user_id, create_user_id, last_update_user_id)
        VALUES (#{code}, #{name}, #{mobile},
        #{email}, #{linkman}, #{business},
        #{provinceCode}, #{cityCode}, #{districtCode},
        #{goodsTableName}, #{saleTableName}, #{partitionCode},
        #{userId}, #{createUserId}, #{lastUpdateUserId})
    </insert>
</mapper>