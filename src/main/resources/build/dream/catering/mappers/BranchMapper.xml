<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.catering.mappers.BranchMapper">
    <insert id="insertMergeUserBranch">
        INSERT INTO merge_user_branch(user_id, tenant_id, tenant_code, branch_id, created_user_id, updated_user_id, updated_remark) VALUES (#{userId}, #{tenantId}, #{tenantCode}, #{branchId}, #{currentUserId}, #{currentUserId}, #{updatedRemark})
    </insert>

    <select id="countUsers" resultType="java.lang.Long">
        SELECT COUNT(1) FROM merge_user_branch
    </select>

    <select id="findAllUserIds" resultType="java.math.BigInteger">
        SELECT user_id FROM merge_user_branch
    </select>

    <select id="findByTenantIdAndUserId" resultType="build.dream.common.catering.domains.Branch">
        SELECT * FROM branch WHERE id = (SELECT branch_id FROM merge_user_branch WHERE tenant_id = #{tenantId} AND user_id = #{userId} AND deleted = 0) AND tenant_id = #{tenantId} AND deleted = 0;
    </select>

    <select id="obtainAllBindJDDJBranches" resultType="build.dream.common.catering.domains.Branch">
        SELECT
        *
        FROM branch
        WHERE jddj_vender_id IS NOT NULL and jddj_vender_id != ''
        AND jddj_app_key IS NOT NULL and jddj_app_key != ''
        AND jddj_app_secret IS NOT NULL and jddj_app_secret != ''
        AND deleted = 0
    </select>
</mapper>