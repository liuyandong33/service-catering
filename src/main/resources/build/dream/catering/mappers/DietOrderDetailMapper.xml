<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.catering.mappers.DietOrderDetailMapper">
    <insert id="insert" parameterType="build.dream.common.erp.catering.domains.DietOrderDetail" useGeneratedKeys="true" keyProperty="id">
        ${@build.dream.common.utils.DatabaseUtils@generateInsertSql("build.dream.common.erp.catering.domains.DietOrderDetail")}
    </insert>

    <insert id="insertAll" parameterType="java.util.List">
        <bind name="sql" value="@build.dream.common.utils.DatabaseUtils@generateInsertAllSql('build.dream.common.erp.catering.domains.DietOrderDetail')"></bind>
        ${sql[0]}
        <foreach collection="list" index="index" item="item" separator=",">
            ${sql[1]}
        </foreach>
    </insert>

    <select id="update" parameterType="build.dream.common.erp.catering.domains.DietOrderDetail">
        ${@build.dream.common.utils.DatabaseUtils@generateUpdateSql("build.dream.common.erp.catering.domains.DietOrderDetail")}
    </select>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.erp.catering.domains.DietOrderDetail">
        SELECT * FROM diet_order_detail <include refid="build.dream.catering.mappers.CommonMapper.searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.erp.catering.domains.DietOrderDetail">
        SELECT * FROM diet_order_detail <include refid="build.dream.catering.mappers.CommonMapper.searchConditionSql"></include>
    </select>
</mapper>