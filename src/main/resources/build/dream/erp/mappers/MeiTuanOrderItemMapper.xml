<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.erp.mappers.MeiTuanOrderItemMapper">
    <insert id="insert" parameterType="build.dream.common.erp.catering.domains.MeiTuanOrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mei_tuan_order_item(mei_tuan_order_id, app_food_code, box_num,
        box_price, food_name, price,
        sku_id, quantity, unit,
        food_discount, food_property, food_share_fee_charge_by_poi,
        cart_id, create_user_id, last_update_user_id,
        last_update_remark)
        VALUES(#{meiTuanOrderId}, #{appFoodCode}, #{boxNum},
        #{boxPrice}, #{foodName}, #{price},
        #{skuId}, #{quantity}, #{unit},
        #{foodDiscount}, #{foodProperty}, #{foodShareFeeChargeByPoi},
        #{cartId}, #{createUserId}, #{lastUpdateUserId},
        #{lastUpdateRemark})
    </insert>

    <update id="update" parameterType="build.dream.common.erp.catering.domains.MeiTuanOrderItem">
        UPDATE mei_tuan_order_item
        SET mei_tuan_order_id = #{meiTuanOrderId},
            app_food_code = #{appFoodCode},
            box_num = #{boxNum},
            box_price = #{boxPrice},
            food_name = #{foodName},
            price = #{price},
            sku_id = #{skuId},
            quantity = #{quantity},
            unit = #{unit},
            food_discount = #{foodDiscount},
            food_property = #{foodProperty},
            food_share_fee_charge_by_poi = #{foodShareFeeChargeByPoi},
            cart_id = #{cartId},
            create_user_id = #{createUserId},
            last_update_user_id = #{lastUpdateUserId},
            last_update_remark = #{lastUpdateRemark},
            deleted = #{deleted}
        WHERE id = #{id}
    </update>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.erp.catering.domains.MeiTuanOrderItem">
        SELECT * FROM mei_tuan_order_item <include refid="searchConditionSql"></include>
    </select>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.erp.catering.domains.MeiTuanOrderItem">
        SELECT * FROM mei_tuan_order_item <include refid="searchConditionSql"></include>
    </select>

    <sql id="searchConditionSql">
        WHERE
        <foreach collection="searchConditions" index="index" item="searchCondition" separator=" AND ">
            <choose>
                <when test="@org.apache.commons.lang.StringUtils@equalsIgnoreCase(searchCondition.operationSymbol, 'IN')">
                    ${searchCondition.columnName} ${searchCondition.operationSymbol}
                    <foreach collection="searchCondition.searchParameter" index="index" item="item" open="(" separator="," close=")">
                        #{item}
                    </foreach>
                </when>
                <otherwise>
                    ${searchCondition.columnName} ${searchCondition.operationSymbol} #{searchCondition.searchParameter}
                </otherwise>
            </choose>
        </foreach>
    </sql>
</mapper>