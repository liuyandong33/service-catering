<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="build.dream.erp.mappers.DietOrderDetailMapper">
    <insert id="insert" parameterType="build.dream.common.erp.catering.domains.DietOrderDetail">
        INSERT INTO diet_order_detail(diet_order_id, goods_id,
        goods_specification_id, goods_flavor_ids, price,
        discount_amount, payable_amount, amount,
        local_id, local_create_time, local_last_update_time,
        create_user_id, last_update_user_id, last_update_remark)
        VALUES(#{dietOrderId}, #{goodsId},
        #{goodsSpecificationId}, #{goodsFlavorIds}, #{price},
        #{discountAmount}, #{payableAmount}, #{amount},
        #{localId}, #{localCreateTime}, #{localLastUpdateTime},
        #{createUserId}, #{lastUpdateUserId}, #{lastUpdateRemark})
    </insert>

    <insert id="insertAll" parameterType="java.util.List">
        INSERT INTO diet_order_detail(diet_order_id, goods_id,
        goods_specification_id, goods_flavor_ids, price,
        discount_amount, payable_amount, amount,
        local_id, local_create_time, local_last_update_time,
        create_user_id, last_update_user_id, last_update_remark)
        VALUES
        <foreach collection="list" index="index" item="dietOrderDetail" separator=",">
            (#{dietOrderDetail.dietOrderId}, #{dietOrderDetail.goodsId},
            #{dietOrderDetail.goodsSpecificationId}, #{dietOrderDetail.goodsFlavorIds}, #{dietOrderDetail.price},
            #{dietOrderDetail.discountAmount}, #{dietOrderDetail.payableAmount}, #{dietOrderDetail.amount},
            #{dietOrderDetail.localId}, #{dietOrderDetail.localCreateTime}, #{dietOrderDetail.localLastUpdateTime},
            #{dietOrderDetail.createUserId}, #{dietOrderDetail.lastUpdateUserId}, #{dietOrderDetail.lastUpdateRemark})
        </foreach>
    </insert>

    <select id="update" parameterType="build.dream.common.erp.catering.domains.DietOrderDetail">
        UPDATE diet_order_detail
        SET diet_order_id = #{dietOrderId},
        goods_id = #{goodsId},
        goods_specification_id = #{goodsSpecificationId},
        goods_flavor_ids = #{goodsFlavorIds},
        price = #{price},
        discount_amount = #{discountAmount},
        payable_amount = #{payableAmount},
        amount = #{amount},
        local_id = #{localId},
        local_create_time = #{lastUpdateUserId},
        local_last_update_time = #{lastUpdateRemark},
        create_user_id = #{createUserId},
        last_update_user_id = #{lastUpdateUserId},
        last_update_remark = #{lastUpdateRemark},
        deleted = #{deleted}
        WHERE id = #{id}
    </select>

    <select id="find" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.erp.catering.domains.DietOrderDetail">
        SELECT * FROM activity <include refid="searchConditionSql"></include>
    </select>

    <select id="findAll" parameterType="build.dream.common.utils.SearchModel" resultType="build.dream.common.erp.catering.domains.DietOrderDetail">
        SELECT * FROM activity <include refid="searchConditionSql"></include>
    </select>

    <sql id="searchConditionSql">
        WHERE
        <foreach collection="searchConditions" index="index" item="searchCondition" separator=" AND ">
            <choose>
                <when test="@org.apache.commons.lang.StringUtils@equalsIgnoreCase(searchCondition.operationSymbol, 'IN')">
                    ${searchCondition.columnName} ${searchCondition.operationSymbol}
                    <foreach collection="searchCondition.searchParameter" index="index" item="item" open="(" separator="," close=")">
                        #{item}
                    </foreach>
                </when>
                <otherwise>
                    ${searchCondition.columnName} ${searchCondition.operationSymbol} #{searchCondition.searchParameter}
                </otherwise>
            </choose>
        </foreach>
    </sql>
</mapper>